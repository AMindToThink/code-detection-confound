# data artifacts (regenerable; can be large)
data/*.parquet
data/repos/
# regenerable from external/CodeGPTSensor/dataset/ via scripts/15_format_hmcorp.py
# (~1.3 GB; takes ~30 min to rebuild)
data/hmcorp/
# training-run checkpoints + telemetry (large; reproducible from scripts/18_*)
results/cgs/
# secrets (never commit API keys)
.env
.env.*
# python
__pycache__/
*.pyc
.venv/
# keep results (figures/tables) tracked
# latex build intermediates (keep paper.tex, macros.tex, paper.pdf)
paper/*.aux
paper/*.fls
paper/*.fdb_latexmk
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.synctex.gz
results/train/
.agent-status.flag
wandb/
external/
